const { tx } = require('../Aula1/texto');

//const regExp1 = /Andressa|João|Tais/ig;
//const regExp2 = regExp1.exec(tx);

//console.log(tx.match(regExp1)); // mostrar so o que eu quero ver
//console.log(tx)

//replace => subistituindo 
//console.log(tx.replace(/João/gi, 'Tarcisio'))


/*
//replace colocando o nome entr parentes escreve html
console.log(tx.replace(/(João)/gi, '<b>$1</b>'))
// podendo tambem coloccar uma função
console.log(tx.replace(/(João|Tais)/gi, function (input) {
  return "********" + input.toUpperCase() + "*********";
}))
*/

// Aula acima finalizada 